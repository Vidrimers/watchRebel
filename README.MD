# watchRebel - Социальная сеть для любителей кино и сериалов

Веб-приложение для любителей фильмов и сериалов с интеграцией TMDb API и Telegram Bot.

## Описание

watchRebel - это социальная платформа, где пользователи могут:
- 🎬 Вести личные коллекции фильмов и сериалов
- ⭐ Оценивать и писать отзывы о просмотренном
- 📝 Создавать пользовательские списки для категоризации контента
- 👥 Добавлять друзей и следить за их активностью
- 📺 Отслеживать прогресс просмотра сериалов
- 💬 Делиться мнениями на личной стене
- 🔔 Получать уведомления в Telegram
- 🎨 Выбирать из множества тем оформления

## Структура проекта

```
watchrebel/
├── client/          # React frontend (Vite)
│   ├── src/
│   │   ├── components/    # React компоненты
│   │   ├── pages/         # Страницы приложения
│   │   ├── store/         # Redux store
│   │   ├── services/      # API клиенты
│   │   └── styles/        # CSS модули и темы
│   └── vite.config.js
├── server/          # Express backend
│   ├── src/
│   │   ├── routes/        # API endpoints
│   │   ├── services/      # Бизнес-логика
│   │   ├── database/      # SQLite база данных
│   │   └── middleware/    # Express middleware
│   └── rebel.db           # База данных SQLite
├── telegram-bot/    # Telegram бот
│   └── src/
│       └── index.js       # Логика бота
├── docs/            # Документация
│   ├── API.md             # API документация
│   ├── QUICK_START.md     # Быстрый старт
│   └── PRODUCTION_SETUP.md # Production развертывание
└── .env            # Переменные окружения
```

## Требования

- Node.js >= 18.0.0
- npm >= 9.0.0
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))
- TMDb API Key (получить на [themoviedb.org](https://www.themoviedb.org/settings/api))

## Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd watchrebel
```

### 2. Установка зависимостей

```bash
npm install
```

Это автоматически установит зависимости для всех workspaces (client, server, telegram-bot).

### 3. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните необходимые значения:

```bash
cp .env.example .env
```

**Обязательные переменные:**

```env
# Telegram Bot
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here  # Получить у @BotFather
TELEGRAM_ADMIN_ID=137981675                      # Ваш Telegram ID

# TMDb API
TMDB_API_KEY=your_tmdb_api_key_here             # Получить на themoviedb.org

# Session
SESSION_SECRET=your_random_secret_here           # Любая случайная строка

# Server
PORT=1313                                        # Порт сервера
PUBLIC_URL=http://localhost:1313                # URL для production
```

**Опциональные переменные:**

```env
# Webhook (только для production)
WEBHOOK_URL=https://your-domain.com
WEBHOOK_PORT=8443

# Database
DB_PATH=./server/rebel.db

# Logging
LOG_LEVEL=INFO
LOG_DIR=./logs
```

### 4. Инициализация базы данных

База данных SQLite создастся автоматически при первом запуске сервера. Для применения миграций:

```bash
npm run migrate
```

## Запуск проекта

### Режим разработки

**Запуск всех компонентов одновременно:**
```bash
npm run dev
```

Это запустит:
- Frontend (React) на http://localhost:3000
- Backend (Express) на http://localhost:1313
- Telegram Bot в режиме polling

**Запуск отдельных компонентов:**

Frontend (React + Vite):
```bash
npm run dev:client
```

Backend (Express + nodemon):
```bash
npm run dev:server
```

Telegram Bot (nodemon):
```bash
npm run dev:tg
```

### Режим production

Backend сервер:
```bash
npm run start
```

Telegram Bot:
```bash
npm run tg
```

Frontend (сборка):
```bash
cd client
npm run build
```

Статические файлы будут в `client/dist/`

## Доступ к приложению

После запуска:

- **Frontend:** http://localhost:3000
- **Backend API:** http://localhost:1313/api
- **Health Check:** http://localhost:1313/api/health
- **Тестовая страница:** http://localhost:3000/integration-test

## Авторизация

1. Откройте Telegram и найдите вашего бота
2. Отправьте команду `/start`
3. Бот отправит вам ссылку для входа на сайт
4. Перейдите по ссылке - вы будете автоматически авторизованы

## Основные функции

### Управление контентом
- 🔍 Поиск фильмов и сериалов через TMDb API
- 📋 Создание пользовательских списков (например: "Любимые", "Посмотреть позже")
- ⭐ Оценка контента по шкале от 1 до 10
- 📝 Написание отзывов и текстовых постов
- 📺 Отслеживание прогресса просмотра сериалов (сезоны/серии)

### Социальные функции
- 👥 Добавление друзей
- 🔔 Уведомления о действиях друзей в Telegram
- 😊 Реакции эмоджи на посты
- 📰 Лента активности друзей
- 🔗 Реферальная система для приглашения друзей

### Персонализация
- 🎨 8 тем оформления (светлые и темные)
- 👤 Настройка профиля
- 🖼️ Аватары из Telegram

### Администрирование
- 👮 Админ-панель для управления пользователями
- 🚫 Система модерации (блокировка, бан постов)
- 📢 Объявления для всех пользователей
- 💾 Резервное копирование базы данных

## API Документация

Полная документация API доступна в [docs/API.md](./docs/API.md)

Основные endpoints:
- `/api/auth/*` - Аутентификация
- `/api/users/*` - Управление пользователями
- `/api/media/*` - Поиск и информация о контенте
- `/api/lists/*` - Пользовательские списки
- `/api/watchlist/*` - Хочу посмотреть
- `/api/ratings/*` - Оценки
- `/api/wall/*` - Стена активности
- `/api/notifications/*` - Уведомления
- `/api/admin/*` - Администрирование

## Тестирование

### Автоматические тесты

Запуск всех тестов:
```bash
# Backend тесты
cd server
npm test

# Frontend тесты
cd client
npm test

# Telegram Bot тесты
cd telegram-bot
npm test
```

### Интеграционные тесты

```bash
# Автоматический тест интеграции
node test-integration.js

# UI тесты в браузере
# Откройте: http://localhost:3000/integration-test
```

### Property-Based тесты

Проект использует property-based testing с библиотекой fast-check:

```bash
# Запуск property тестов
cd server
npm test -- --testPathPattern=property

cd client
npm test -- --testPathPattern=property
```

## Production развертывание

### Подготовка к деплою

1. Обновите переменные окружения для production:
```env
NODE_ENV=production
PUBLIC_URL=https://your-domain.com
WEBHOOK_URL=https://your-domain.com
API_URL=https://your-domain.com
```

2. Соберите frontend:
```bash
cd client
npm run build
```

3. Настройте веб-сервер (nginx/apache) для раздачи статики из `client/dist/`

### Быстрый деплой

```bash
export NODE_ENV=production
./scripts/deploy.sh
```

### Настройка Telegram Webhook

Для production рекомендуется использовать webhook вместо polling:

```bash
# Установить webhook
./scripts/setup-webhook.sh set

# Проверить статус
./scripts/setup-webhook.sh info

# Удалить webhook (вернуться к polling)
./scripts/setup-webhook.sh delete
```

### Миграции базы данных

```bash
npm run migrate
```

### Резервное копирование

Автоматическое резервное копирование:
```bash
npm run backup
```

Бэкапы сохраняются в `server/backups/`

### Подробная документация

Полное руководство по развертыванию: [docs/PRODUCTION_SETUP.md](./docs/PRODUCTION_SETUP.md)

## Управление процессами

### PM2 (рекомендуется для production)

```bash
# Установка PM2
npm install -g pm2

# Запуск с помощью ecosystem.config.js
pm2 start ecosystem.config.js

# Управление
pm2 status
pm2 logs
pm2 restart all
pm2 stop all
```

### Systemd

Создайте systemd service для автозапуска:

```bash
sudo nano /etc/systemd/system/watchrebel.service
```

```ini
[Unit]
Description=watchRebel Social Network
After=network.target

[Service]
Type=simple
User=your-user
WorkingDirectory=/path/to/watchrebel
ExecStart=/usr/bin/npm run start
Restart=always

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl enable watchrebel
sudo systemctl start watchrebel
```

## Технологии

### Frontend
- **React 18** - UI библиотека
- **Vite** - Быстрый bundler и dev server
- **Redux Toolkit** - Управление состоянием
- **React Router** - Маршрутизация
- **Axios** - HTTP клиент
- **CSS Modules** - Изолированные стили

### Backend
- **Node.js** - Runtime
- **Express** - Web framework
- **SQLite** (better-sqlite3) - База данных
- **TMDb API** - Данные о фильмах и сериалах
- **JWT** - Аутентификация

### Telegram Bot
- **node-telegram-bot-api** - Telegram Bot API

### Testing
- **Jest** - Test runner
- **fast-check** - Property-based testing
- **React Testing Library** - Тестирование компонентов

### DevOps
- **nodemon** - Автоперезагрузка при разработке
- **concurrently** - Запуск нескольких процессов
- **PM2** - Process manager для production

## Структура базы данных

Проект использует SQLite с следующими основными таблицами:

- `users` - Пользователи
- `sessions` - Сессии авторизации
- `custom_lists` - Пользовательские списки
- `list_items` - Контент в списках
- `watchlist` - Хочу посмотреть
- `ratings` - Оценки контента
- `wall_posts` - Записи на стене
- `reactions` - Реакции на посты
- `episode_progress` - Прогресс просмотра сериалов
- `friends` - Связи между пользователями
- `notifications` - Уведомления
- `referrals` - Реферальная система
- `moderation_actions` - История модерации

Подробная схема: [server/src/database/README.md](./server/src/database/README.md)

## Разработка

### Архитектура

Проект использует monorepo структуру с npm workspaces:

```
watchrebel/
├── client/          # Frontend workspace
├── server/          # Backend workspace
└── telegram-bot/    # Bot workspace
```

Каждый workspace имеет свой `package.json` и может разрабатываться независимо.

### Для разработчиков

Если вы хотите внести вклад в проект, прочитайте [CONTRIBUTING.md](./CONTRIBUTING.md) - руководство для разработчиков с:
- Соглашениями о коде
- Git workflow
- Инструкциями по добавлению функций
- Примерами работы с Redux и БД
- Чек-листами перед коммитом

### Добавление новых зависимостей

```bash
# Для всего проекта
npm install <package>

# Для конкретного workspace
npm install <package> --workspace=client
npm install <package> --workspace=server
npm install <package> --workspace=telegram-bot
```

### Соглашения о коде

- Комментарии в коде на русском языке
- Названия переменных/функций на английском
- Используйте CSS Modules для стилей компонентов
- Следуйте структуре Redux Toolkit (slices)
- API endpoints следуют REST conventions

### Git workflow

```bash
# Создание feature branch
git checkout -b feature/название-функции

# Коммит изменений
git add .
git commit -m "Описание изменений на русском"

# Push и создание PR
git push origin feature/название-функции
```

## Troubleshooting

### Проблемы с установкой

**Ошибка: `Cannot find module`**
```bash
# Переустановите зависимости
rm -rf node_modules client/node_modules server/node_modules telegram-bot/node_modules
npm install
```

**Ошибка: `EADDRINUSE` (порт занят)**
```bash
# Найдите процесс на порту 1313
lsof -i :1313
# Или на Windows
netstat -ano | findstr :1313

# Убейте процесс или измените PORT в .env
```

### Проблемы с Telegram Bot

**Бот не отвечает**
1. Проверьте `TELEGRAM_BOT_TOKEN` в `.env`
2. Убедитесь что бот запущен: `npm run dev:tg`
3. Проверьте логи: `tail -f logs/app.log`

**Webhook не работает**
```bash
# Проверьте статус webhook
./scripts/setup-webhook.sh info

# Переключитесь на polling для разработки
# Удалите WEBHOOK_URL из .env
```

### Проблемы с базой данных

**База данных заблокирована**
```bash
# Остановите все процессы
pm2 stop all
# или
pkill -f node

# Перезапустите
npm run dev
```

**Нужно сбросить базу данных**
```bash
# Создайте бэкап
npm run backup

# Удалите базу
rm server/rebel.db

# Запустите миграции
npm run migrate
```

### Проблемы с TMDb API

**Ошибка: `Invalid API key`**
- Проверьте `TMDB_API_KEY` в `.env`
- Убедитесь что ключ активен на themoviedb.org

**Ошибка: `Rate limit exceeded`**
- TMDb ограничивает ~40 запросов в секунду
- Подождите несколько секунд и повторите

## Документация

### Основная документация
- 📖 [README.MD](./README.MD) - Главная страница (этот файл)
- 🚀 [Руководство по установке](./docs/INSTALLATION.md) - Пошаговая установка
- 📁 [Структура проекта](./docs/PROJECT_STRUCTURE.md) - Описание файлов и директорий
- ⚡ [Быстрый старт](./docs/QUICK_START.md) - Начало работы

### API и разработка
- 🔌 [API Документация](./docs/API.md) - Полное описание всех endpoints
- 🏗️ [Production Setup](./docs/PRODUCTION_SETUP.md) - Развертывание на сервере
- 🧪 [Интеграционное тестирование](./docs/INTEGRATION_TESTING.md) - Тестирование

### Функции и интеграции
- 🤖 [Telegram Integration](./docs/TELEGRAM_INTEGRATION.md) - Настройка бота
- 📧 [Email Setup](./docs/email/EMAIL_README.md) - Настройка SMTP для email-регистрации и восстановления пароля
- 👮 [Модерация](./docs/MODERATION_FEATURE.md) - Функции модерации
- 📊 [Результаты тестов](./docs/INTEGRATION_TEST_RESULTS.md) - Отчеты о тестировании

## Поддержка

Для вопросов и предложений:
- Telegram: @your_username
- Email: your_email@example.com

## Лицензия

Частный проект. Все права защищены.

## Авторы

- Разработка: Ваше имя
- Дизайн: Ваше имя

## Благодарности

- [TMDb](https://www.themoviedb.org/) - За предоставление API с данными о фильмах
- [Telegram](https://telegram.org/) - За Bot API
- Сообщество React и Node.js

---

**watchRebel** - Ваша социальная сеть для любителей кино и сериалов 🎬
