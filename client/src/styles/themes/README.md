# Темы оформления watchRebel

Этот документ описывает все доступные темы оформления в приложении watchRebel.

## Доступные темы

### 1. Light Cream (Светлая кремовая) ☀️
**Значение:** `light-cream`

Светлая кремовая тема по умолчанию. Мягкие пастельные тона с кремовым фоном.

**Основные цвета:**
- Фон: `#f5f5f0` (кремовый)
- Акцент: `#6366f1` (индиго)
- Текст: `#2d2d2d` (темно-серый)

### 2. Dark (Темная) 🌙
**Значение:** `dark`

Классическая темная тема с глубокими серыми тонами.

**Основные цвета:**
- Фон: `#0f0f0f` (очень темный)
- Акцент: `#818cf8` (светлый индиго)
- Текст: `#e8e8e8` (светло-серый)

### 3. Material Light (Материал светлая) 🎨
**Значение:** `material-light`

Светлая тема в стиле Google Material Design с характерными цветами и тенями.

**Основные цвета:**
- Фон: `#ffffff` (чистый белый)
- Акцент: `#1976d2` (Material Blue 700)
- Текст: `rgba(0, 0, 0, 0.87)` (высокая контрастность)

**Особенности:**
- Типографика Material Design с правильными уровнями прозрачности
- Многослойные тени (elevation) в стиле Material
- Цветовая палитра из Material Design Guidelines
- Эффект ripple для кнопок
- Плавные переходы между состояниями

### 4. Material Dark (Материал темная) 🌐
**Значение:** `material-dark`

Темная тема в стиле Google Material Design с правильными уровнями elevation.

**Основные цвета:**
- Фон: `#121212` (Material Dark background)
- Акцент: `#90caf9` (Material Blue 200)
- Текст: `rgba(255, 255, 255, 0.87)` (высокая контрастность)

**Особенности:**
- Система elevation с разными уровнями поверхностей
- Правильные уровни прозрачности текста для темной темы
- Усиленные тени для темного фона
- Цветовая палитра из Material Design Dark Theme Guidelines
- Эффект overlay для разных уровней elevation

### 5. Die my Darling (Кроваво-красная) 🩸
**Значение:** `die-my-darling`

Драматичная тема с кроваво-красными акцентами и полупрозрачными элементами.

**Основные цвета:**
- Фон: `#0a0000` (очень темный красный)
- Акцент: `#cc0000` (кроваво-красный)
- Текст: `#ffebeb` (светло-красноватый)

**Особенности:**
- Полупрозрачные фоны
- Эффект свечения на интерактивных элементах
- Красноватый фильтр для изображений

### 6. Steam (В стиле Steam) 🎮
**Значение:** `steam`

Тема в стиле игровой платформы Steam с характерными синими тонами.

**Основные цвета:**
- Фон: `#1b2838` (темно-синий Steam)
- Акцент: `#66c0f4` (фирменный синий Steam)
- Вторичный акцент: `#5c7e10` (зеленый Steam)

**Особенности:**
- Градиентные кнопки в стиле Steam
- Эффект свечения для активных элементов
- Градиентный фон

### 7. Discord (В стиле Discord) 💬
**Значение:** `discord`

Тема в стиле мессенджера Discord с характерными серыми и синими тонами.

**Основные цвета:**
- Фон: `#36393f` (темно-серый Discord)
- Акцент: `#5865f2` (фирменный синий Discord)
- Вторичный акцент: `#3ba55d` (зеленый Discord)

**Особенности:**
- Стиль кнопок Discord
- Эффект подсветки при наведении на сообщения
- Широкий скроллбар в стиле Discord

### 8. Metal and Glass (Металл и стекло) ✨
**Значение:** `metal-and-glass`

Современная тема с эффектами стекла и металлического блеска.

**Основные цвета:**
- Фон: `#1a1d23` (темный металлический)
- Акцент: `#60a5fa` (голубой)
- Вторичный акцент: `#94a3b8` (серебристый)

**Особенности:**
- Эффект размытого стекла (`backdrop-filter: blur()`)
- Полупрозрачные элементы
- Металлический градиент для заголовков
- Эффект свечения для активных элементов

### 9. Cyberpunk (Киберпанк) 🌃
**Значение:** `cyberpunk`

Яркая неоновая тема в стиле киберпанк с эффектами свечения.

**Основные цвета:**
- Фон: `#0a0e27` (очень темный синий)
- Акцент: `#f0f` (неоновый пурпурный)
- Вторичный акцент: `#0ff` (неоновый циан)

**Особенности:**
- Яркие неоновые цвета
- Анимация неонового свечения
- Эффект сканирующей линии
- Прозрачные кнопки с неоновыми границами

### 10. Dark Neon Obsidian (Темный неоновый обсидиан) 💎
**Значение:** `dark-neon-obsidian`

Элегантная темная тема с неоновыми мятными акцентами.

**Основные цвета:**
- Фон: `#05060a` (очень темный обсидиан)
- Акцент: `#3cffc4` (неоновый мятный)
- Вторичный акцент: `#2dd4a8` (темно-мятный)

**Особенности:**
- Минималистичный темный фон
- Неоновые мятные акценты
- Эффект пульсации для уведомлений
- Эффект обсидианового стекла

## Использование тем

### В компонентах React

Темы автоматически применяются через Redux store:

```javascript
import { useAppDispatch } from './hooks/useAppDispatch';
import { setTheme } from './store/slices/themeSlice';

function MyComponent() {
  const dispatch = useAppDispatch();
  
  const handleThemeChange = (themeName) => {
    dispatch(setTheme(themeName));
  };
  
  return (
    <button onClick={() => handleThemeChange('cyberpunk')}>
      Применить Cyberpunk тему
    </button>
  );
}
```

### В CSS

Все темы используют CSS переменные, которые можно использовать в любом компоненте:

```css
.my-component {
  background-color: var(--bg-primary);
  color: var(--text-primary);
  border: 1px solid var(--border-color);
}

.my-button {
  background-color: var(--accent-primary);
  color: var(--text-inverse);
}
```

### Сохранение темы

Тема автоматически сохраняется в:
1. **localStorage** - для быстрого применения при следующем визите
2. **База данных** - через API при обновлении профиля пользователя

## Создание новой темы

Чтобы создать новую тему:

1. Создайте новый CSS файл в `client/src/styles/themes/`
2. Определите все обязательные CSS переменные (см. существующие темы)
3. Добавьте импорт в `client/src/index.css`
4. Добавьте тему в массив `themes` в `ThemeSelector.jsx`
5. Обновите тесты в `theme.property.test.js`

### Обязательные CSS переменные

Каждая тема должна определять следующие переменные:

```css
[data-theme="your-theme-name"] {
  /* Фоны */
  --bg-primary
  --bg-secondary
  --bg-tertiary
  --bg-hover
  --bg-active
  --bg-input
  --bg-disabled
  
  /* Текст */
  --text-primary
  --text-secondary
  --text-tertiary
  --text-disabled
  --text-inverse
  --text-link
  --text-link-hover
  
  /* Акценты */
  --accent-primary
  --accent-primary-hover
  --accent-primary-active
  --accent-secondary
  --accent-tertiary
  
  /* Состояния */
  --color-success
  --color-warning
  --color-error
  --color-info
  
  /* Границы */
  --border-color
  --border-color-light
  --border-color-dark
  --border-color-focus
  
  /* Тени */
  --shadow-xs
  --shadow-sm
  --shadow-md
  --shadow-lg
  --shadow-xl
  
  /* Скроллбар */
  --scrollbar-track
  --scrollbar-thumb
  --scrollbar-thumb-hover
}
```

## Тестирование

Все темы покрыты автоматическими тестами:

```bash
# Property-based тесты
npm test -- --run src/store/slices/__tests__/theme.property.test.js

# Интеграционные тесты
node --test src/styles/themes/__tests__/themes.integration.test.js
```

## Поддержка браузеров

Все темы поддерживают:
- Chrome/Edge (последние версии)
- Firefox (последние версии)
- Safari (последние версии)

Некоторые эффекты (например, `backdrop-filter`) могут не работать в старых браузерах.
