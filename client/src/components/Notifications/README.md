# Компоненты уведомлений

Компоненты для отображения уведомлений о реакциях и активности друзей.

## Компоненты

### NotificationBadge

Значок с количеством непрочитанных уведомлений. Отображается в Sidebar рядом с ссылкой "Уведомления".

**Особенности:**
- Автоматически загружает уведомления при монтировании
- Показывает количество непрочитанных уведомлений
- Скрывается, если нет непрочитанных уведомлений
- Отображает "99+" для количества больше 99
- Имеет анимацию пульсации для привлечения внимания

**Использование:**
```jsx
import { NotificationBadge } from '../components/Notifications';

<NotificationBadge />
```

### NotificationList

Список всех уведомлений пользователя с возможностью отметить как прочитанное.

**Особенности:**
- Отображает уведомления двух типов: реакции и активность друзей
- Показывает иконку в зависимости от типа уведомления
- Форматирует время относительно текущего момента
- Выделяет непрочитанные уведомления
- При клике отмечает уведомление как прочитанное и переходит к связанному контенту
- Адаптивный дизайн для мобильных устройств

**Использование:**
```jsx
import { NotificationList } from '../components/Notifications';

<NotificationList />
```

## Интеграция с Redux

Компоненты используют `notificationsSlice` для управления состоянием:

- `fetchNotifications()` - загрузка уведомлений
- `markAsRead(notificationId)` - отметка уведомления как прочитанного
- `unreadCount` - количество непрочитанных уведомлений
- `notifications` - массив всех уведомлений

## Типы уведомлений

1. **reaction** - реакция на пост пользователя (❤️)
2. **friend_activity** - активность друга (👤)

## Структура уведомления

```javascript
{
  id: string,
  userId: string,
  type: 'reaction' | 'friend_activity',
  content: string,
  relatedUserId?: string,
  relatedPostId?: string,
  isRead: boolean,
  createdAt: Date
}
```

## Стили

Компоненты поддерживают светлую и темную темы через CSS переменные и атрибут `[data-theme]`.

## Requirements

Компоненты реализуют следующие требования:
- 7.3 - Отображение значка уведомлений в правом блоке
- 11.3 - Отображение уведомлений в правом блоке
- 11.4 - Отображение кнопки "Добавить в друзья" на профиле
- 11.5 - Отображение активности друзей на Wall
